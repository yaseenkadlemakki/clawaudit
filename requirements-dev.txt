# ClawAudit test suite dependencies
# Pin to exact versions for reproducible CI builds.

pytest==9.0.2
pytest-cov==7.0.0
pyyaml==6.0.3

# Optional: lint tools invoked by CI (not imported by tests directly)
yamllint==1.38.0
python-dotenv==1.2.2
watchdog>=4.0.0
pydantic>=2.0.0
httpx>=0.27.0
typer[all]>=0.9.0
rich>=13.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pytest-asyncio>=0.23.0

# Backend dependencies (required for tests/backend/ collection)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.30.0
alembic>=1.14.0
websockets>=13.0
python-multipart>=0.0.12
